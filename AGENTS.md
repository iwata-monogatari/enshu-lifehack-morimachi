# 遠州ライフハック／森町ライフハック デザイン維持ルール

## 絵文字・アイコンについて

本サイトでは、控えめな絵文字・アイコンを「スマホで押しやすく見せるための視覚的手がかり」として使用する。

以下は、明確な削除指示がない限り削除禁止。

- 各市町村ライフハックの先頭絵文字
- お城ファビコン
- トップページの3×3入口・13カテゴリ・困りごとの装飾アイコン
- 中項目ページの控えめな装飾絵文字
- フッターの市町村一覧絵文字

絵文字・アイコンは必ず `aria-hidden="true"` または `alt=""` を使い、読み上げ対象外にする。

絵文字は意味の中心ではなく装飾である。絵文字を外すのではなく、アクセシビリティ対応して残すこと。

## 実装依頼の完了条件

- このリポジトリへの作成・追加・修正・更新・実装・反映・公開を依頼された場合、ユーザーがローカル作業、成果物作成、またはPRまでと明示しない限り、本番公開と公開後検証までを一連の作業として扱う。依頼動詞の違いで公開工程を省略しない。
- ローカル実装、ZIP作成、コミット、ブランチへのpushだけで「完了」と報告しない。これらは途中経過として commentary で伝える。
- 完了報告の前に、対象変更だけをステージし、最新 `origin/main` との差分を確認し、全体ビルドと関連監査を通し、production branchへ反映する。
- push成功や一定時間の経過だけでデプロイ完了としない。確認できる場合はCloudflareのproduction deploymentが成功し、Source commit SHAが今回のコミットと一致するまで追跡する。状態を直接確認できない場合は、本番の変更固有内容とローカル成果物の一致を必須とする。
- 公開確認はHTTPステータスだけで合格にしない。キャッシュ回避を行い、変更ページ固有の文言・件数・canonicalと、主要HTML・JS・CSS・画像等の内容またはハッシュを確認する。その後 `scripts/verify_published.py` があれば実行し、全URL・301・404を確認する。
- デプロイ待ち、認証待ち、検証中は作業を終了しない。権限・認証の承認後は謝辞だけで終了せず、`HEAD`、`origin/main`、作業ツリー、Cloudflareの状態を再確認して止まった操作を直ちに再実行し、公開後検証まで続行する。
- 「完了」はproduction deploymentまたは本番成果物のコミット一致、変更固有検証、全体検証の3点が合格し、本番URLを提示できる時点だけに使う。未公開の場合は「実装済み・公開未完了」と明記する。
