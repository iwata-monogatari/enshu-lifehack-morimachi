# 遠州ライフハック／森町ライフハック デザイン維持ルール

## 絵文字・アイコンについて

本サイトでは、控えめな絵文字・アイコンを「スマホで押しやすく見せるための視覚的手がかり」として使用する。

以下は、明確な削除指示がない限り削除禁止。

- 各市町村ライフハックの先頭絵文字
- お城ファビコン
- トップページの3×3入口・13カテゴリ・困りごとの装飾アイコン
- 中項目ページの控えめな装飾絵文字
- フッターの市町村一覧絵文字

絵文字・アイコンは必ず `aria-hidden="true"` または `alt=""` を使い、読み上げ対象外にする。

絵文字は意味の中心ではなく装飾である。絵文字を外すのではなく、アクセシビリティ対応して残すこと。

## 実装依頼の完了条件

- このリポジトリでユーザーが「実装して」「反映して」「公開して」と依頼した場合、ユーザーがローカル作業・成果物作成・PRまでと明示しない限り、本番公開と公開後検証までを一連の作業として扱う。
- ローカル実装、ZIP作成、コミット、ブランチへのpushだけで「完了」と報告しない。これらは途中経過として commentary で伝える。
- 完了報告の前に、対象変更だけをステージし、最新 `origin/main` との差分を確認し、全体ビルドと関連監査を通し、production branchへ反映する。
- Cloudflareの反映を待ち、本番URL、主要静的ファイル、トップからの導線、サイトマップを確認する。`scripts/verify_published.py` がある場合は実行し、全URL・301・404の結果を確認する。
- デプロイ待ち、認証待ち、検証中は作業を終了しない。権限や認証が必要な場合だけ、その一点を具体的に依頼し、解消後に同じ作業を再開する。
- 「完了」は本番URLを提示でき、公開後検証が合格した時点だけに使う。未公開の場合は「実装済み・公開未完了」と明記する。
